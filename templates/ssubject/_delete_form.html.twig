<form method="post" action="{{ path('app_ssubject_delete', {'id': ssubject.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ssubject.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_steacher_delete', {'id': steacher.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ steacher.id) }}">
    <button class="btn">Delete</button>
</form>
